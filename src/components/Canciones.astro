---
import { Icon } from "astro-icon/components";

interface Props {
    song: string;
    artist: string;
    imgURL: string;
}
const { artist, song, imgURL } = Astro.props;
---

<div class="flex flex-row items-center gap-14 bg-amber-50/40 grow object-cover justify-between rounded-full">
    <div>
        <img
            src={imgURL}
            alt=""
            class="w-full size-28 object-contain rounded-full"
        />
    </div>
    <div class="flex flex-col text-center">
        <h1 class="font-bold text-xl">{song}</h1>
        <p>{artist}</p>
    </div>
    <div>
        <Icon
            name="stash:play-duotone"
            width="32"
            height="32"
            class="text-violet-950"
        />
    </div>
</div>
